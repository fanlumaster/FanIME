.vs/
out/
build/
Release/
build64/
build64-release/
build32/
build32-release/
draft/
*.log
CMakeSettings.json
log.txt
.workbuddy/

# Release staging. The helpcodes and the dictionary are the only product inputs that still come
# from outside this repository, and both the release workflow and a local release rehearsal drop
# them beside the components under these names.
/MetasequoiaImeHelpCode/
/MetasequoiaImeDict/
